# Configuración de PostgreSQL
spring.jpa.database=POSTGRESQL
spring.sql.init.platform=posgres
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/expense_note_db
spring.datasource.username=jdev
spring.datasource.password=1234
# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.hibernate.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.generate-ddl=true
# -Configuración de SQL:
# [This property controls whether Spring Boot initialises the 
#  database with SQL scripts (such as schema.sql and data.sql)]
#spring.sql.init.mode=never
# -Configuración de la aplicación:
# [Beans Overwritting(last bean overwrite to last one)]
#spring.main.allow-bean-definition-overriding=true
# -Inicialización de la base de datos:
# [Delay the initialisation of the database until 
#  JPA (Java Persistence API) has completed its configuration]
#spring.jpa.defer-datasource-initialization=true
# -Configuración JWT
app.jwt.secret=xxxxxxxxxxxxxxxxxxxx

#spring.datasource.url=jdbc:postgresql://<dbhost>:<dbport>/<dbname>
#spring.datasource.username=<username>
#spring.datasource.password=<password>
#sslmode=disable
#spring.main.allow-circular-references=true
###spring.profiles.active=dev